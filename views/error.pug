extends layout

block content
  div(class="flex flex-col content-center w-full")
    div(class="m-auto bg-white w-11/12 mt-5")
      h1(class="pl-16 text-4xl font-bold")= message
      h2(class="mt-5")= error.status
      pre #{error.stack}